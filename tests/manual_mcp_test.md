# MCP AI共同編集 手動 E2E チェックリスト

自動テスト: `npm test -- --run` (フロント 94件)、`cargo test --lib` (Rust 33件)。
以下は実機での確認手順と結果の記録用。

## 自動E2E済み (2026-09-17, 実 exe + 実 HTTP, ヘッドレス)

- HTTP initialize → 200 + serverInfo + Mcp-Session-Id 発行
- tools/list → 13ツール
- tools/call get_document → 実エディタの内容が返る (フロントブリッジ往復)
- set_cursor / insert_text → 実エディタに反映、get_document で確認、dirty:true
- 2本目 initialize → -32002 (既存AI名付きで拒否)
- disconnect → 解放後に再 initialize 成功
- セッション一致→200 / 不一致→400
- `markink.exe mcp-stdio` (GUI未起動) → 日本語エラー + exit 2
- GUI起動 (有効時) → 127.0.0.1:42110 (WS) + 42120 (HTTP) で待ち受け
- なお E2E 中に発見した実バグ1件 (修正済み):
  `.setup()` 直下での Tokio コンテキスト外 `from_std` panic → フロントからの
  `mcp_autostart` コマンド経由に変更

## 前提

- ビルド済み `src-tauri/target/release/markink.exe` を使用する
- インターネット遮断環境でも実施する (⑤)

## ① 単独編集での AI 操作 (一部は自動E2E済み。目視のみ残)

- [x] (自動)stdio initialize → 接続成功
- [x] (自動)`get_document` / `search` 位置 / `insert_text` 反映
- [x] (自動)`set_cursor` 応答 (紫カーソルの目視は未)
- [ ] `set_cursor` で紫の `AI: …` カーソルが目視できること
- [ ] `replace_range` 反映と `Ctrl+Z` 取消 (単体テスト済み、実機目視のみ)
- [ ] 全文置換で確認ダイアログが出て、拒否で中断・許可で適用される
- [ ] `notify_human` で右下トーストが出る
- [ ] パネルの接続状態表示・切断・トークン再生成・コピーボタン

結果: ____ / 日付: ____

## ② 2端末共同編集 + AI

- [ ] 2端末で同じ文書の共同編集を開始する
- [ ] 片方の端末に AI を接続する
- [ ] AI の編集がもう片方の端末に同期される
- [ ] もう片方の端末にも AI カーソル (`AI: …`) が表示される
- [ ] 人間同士のカーソル名ラベルが AI 接続で上書きされない

結果: ____ / 日付: ____

## ③ 2本目の AI は拒否される (プロトコルは自動E2E済み)

- [x] (自動)AI-A 接続中に AI-B が initialize → エラー (理由に AI-A の名前を含む)
- [x] (自動)AI-A の接続は維持される
- [x] (自動)AI-A 切断後に AI-B が接続できる
- [ ] 拒否時に人間の画面へトースト通知が出る (目視)

結果: ____ / 日付: ____

## ④ HTTP 接続 (プロトコルは自動E2E済み)

- [x] (自動)パネルの URL + Bearer トークンで initialize → セッションIDが返る
- [x] (自動)トークン違いは 401、古いセッションIDは 400
- [x] (自動)`tools/list` に 13 ツールが含まれる
- [x] (自動)`tools/call` で文書を読み書きできる
- [ ] 実クライアント (opencode / Cursor) からの接続確認

結果: ____ / 日付: ____

## ⑤ インターネット遮断環境

- [ ] ネットワークを切断した状態で ① の主要項目 (接続・読み・書き・カーソル) が動作する

結果: ____ / 日付: ____

## ⑥ セキュリティ目視

- [ ] 無効化中は 42110-42129 の待ち受けが無い (リッスン確認)
- [ ] 待ち受けは 127.0.0.1 のみ (0.0.0.0 では無い)
- [ ] 設定ファイル (`%LOCALAPPDATA%\jp.markink.app\settings.json`) に文書本文が含まれない

結果: ____ / 日付: ____

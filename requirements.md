# ローカル 軽量　WYSIWYGマークダウンエディタ 要件定義

## 1. 目的

プログラミング経験がない人でも、Wordのような感覚で文書を作成・編集でき、内容をマークダウン（`.md`）としてローカルに保存できるデスクトップアプリを作る。

利用者はマークダウン記法を覚えなくてよい。必要に応じて、作成されたマークダウンを確認・編集できるようにする。

リアルタイム共同編集機能があり、複数人で同じファイルを同時に編集できる

## 2. 想定利用者

- 日報、議事録、手順書、メモを作る一般利用者
- LLMに仕様書を書かせ、リアルタイムに仕様書を編集したい利用者
- プロジェクトメンバーでオンラインミーティング時のホワイトボードとしたい利用者
- AIアシスタントに草稿を書かせ、人間がその場で直したい利用者
- マークダウン形式での納品・共有が必要だが、記法を覚えたくない利用者
- 閉域の社内LANで文書を管理したい利用者

## 3. 提供価値

- リアルタイム共同編集がインターネット接続なしのLAN環境、サーバなしで可能
- AIとの共同編集：エディタ本体がMCPサーバになり、AIが人間参加者と同じように文章を読み、書き、カーソルを動かせる（ローカル接続のみ、追加のクラウドサービス不要）
- 見たまま編集：太字、見出し、箇条書き、画像などをボタン操作で作成できる
- ローカル完結：共同編集しない場合は、文書と画像を端末内に保存し、通常利用ではアカウントやネット接続を不要とする
- 互換性：標準的なMarkdownファイルとして書き出せる
- 安心感：自動保存、変更の分かりやすい表示、誤操作の取り消しを備える
- 導入の容易さ：管理者権限なしで利用を開始でき、共有のネットワークドライブからも起動できる

### 4 初心者向けの使いやすさ

- 最初に「新規作成」「ファイルを開く」を大きく表示する
- 初期版の書式ツールバーはアイコン表示とし、日本語の操作名・用途のツールチップとアクセシブル名を付ける。後続版では設定からテキスト表示に切り替えられるようにする
- 空の文書には入力例を表示する
- ショートカットを知らなくても全操作をメニューから実行できる
- エラーは専門用語を避け、次に取るべき行動を示す
- 画面デザインはdesign/markdown-editor.svgをベースにする

## 5. 非機能要件

- 対応OSはWindows 10 / 11（64ビット）のみとする
- 通常利用時にインターネット接続、アカウント作成、クラウド保存を要求しない
- 初回起動後を含め、インターネット未接続の環境で全ての基本機能を利用できる
- フォント、変換処理、ヘルプなど、基本利用に必要な要素はアプリに同梱し、外部URLへの読み込みを必要としない
- アップデート確認や利用状況の送信は初期状態で行わない
- 起動から編集可能になるまでを3秒以内（一般的な業務用PC、初回起動を除く）とする
- 5MB・およそ5万文字のMarkdown文書を、操作中に目立つ遅延なく編集できる
- 文章入力、書式変更、Undo / Redoは通常操作で100ms以内に反応することを目標とする
- 画像の読み込みやMarkdown変換など時間のかかる処理は、編集操作を止めない
- アプリが異常終了しても、復元機能を有効にしている場合は、次回起動時に自動保存済み内容の復元を提案する（復元機能は既定で無効。設定で有効・無効を切り替えられる）
- 単独編集ではファイル内容を外部送信しない。明示的に開始・参加した共同編集では、許可された参加端末へ文書更新を送信する。公開クラウドへの自動送信はしない
- 日本語入力（IME）を正常に扱える


## 6. 主要な利用シナリオ

### 議事録を作る

利用者は「新規作成」を選び、見出しボタンと箇条書きボタンで議事録を書く。画像をドラッグ＆ドロップで貼り、保存先を選ぶ。アプリは`meeting.md`と画像用フォルダを作成する。

### 既存Markdownを更新する

利用者は`.md`ファイルを開く。アプリは内容を読みやすく表示し、文章や箇条書きを直接編集できる。保存すると、対応できる範囲で元のMarkdown構造を保ちながら更新する。

### AIと共同編集する

利用者は設定でAI接続を有効にし、表示される接続情報をAIクライアント（Claude Desktopなど）に登録する。AIが文書を読み、文章を追記・修正すると、人間の画面にもリアルタイムに反映され、「AI: クライアント名」のカーソルが動いて何をしているか分かる。危険な大きな置換は承認モードで人間が確認してから適用できる。

## 7. 技術・OSS利用方針

### 7.1 採用方針

- Windows専用のデスクトップアプリとして配布する
- 軽快さを優先し、常時バックグラウンド同期、ログイン、クラウド連携は初期版に含めない。共同編集の同期は利用者が開始・参加した文書セッションに限定する
- WYSIWYG編集部は、実績のあるOSSを活用し、独自実装を最小限に抑える
- OSSのライセンス表示および再配布条件を満たす

### 7.2 採用する技術

- Tauri
- ProseMirror
- WebRTC
- Yjs
- 【重要】SMBファイル共有で共同編集を可能とする
- MCP（Model Context Protocol）サーバ：エディタ本体がツール公開側となり、AI客户端を接続する


### 7.3 開発方針

- 自動テストを実行し、テストが終了するまでそのタスクは終わらないものとする

### 7.4 配布方式

- Windowsのポータブル配布を基本とする。利用者は展開済みフォルダ内の実行ファイルを起動するだけで使える
- 配布フォルダはローカルドライブ、USBメモリ、ネットワークドライブに配置できる
- インストーラー版を追加する場合も、管理者権限を不要とする「ユーザー単位インストール」に限定する
- ネットワークドライブからの実行可否は、Windows Defender、SmartScreen、組織の実行制御ポリシーに影響される。アプリ自身のエラーとポリシーによる制限を区別して案内する

## 8. 確定事項

- 製品名：markink
- 対応OS：Windows 10 / 11のみ
- 編集形式：GFM
- 動作形態：単独編集はローカル完結。共同編集はSMB共有と参加端末間のネットワークを利用し、インターネット接続を必須としない
- 開発方針：Tauriを採用し、編集画面にはProseMirrorを採用する
- 優先順位：初心者が使いやすいこと、軽快に動作すること、GFM互換性
- 配布方式：管理者権限不要のポータブル配布を基本とする
- ネットワークドライブ：アプリの配置・起動を可能にする。設定と自動保存は各利用者のローカル領域に置く
- オフライン利用：初回起動後も含め、基本機能をインターネット接続なしの社内LANで使えることを前提とする。
- 初期版の書き出し：HTMLに対応する。PDF / Wordは対象外とする
- 文書管理：単一文書のみとし、フォルダ単位の管理機能は対象外とする
- AI共同編集：markink本体がMCPサーバとなる。参加AIは同時1本に限定し、localhostのみで待ち受け（既定は無効）。AIカーソルにはAI名を表示する（第10章）

## 9. リアルタイム共同編集

### 9.1 対象と前提

- ProseMirror、Tauri、WebRTC、Yjs、SMBを用いた共同編集を本体に組み込む。
- 初期対象は同一組織のLAN／VPN内で、同じSMB共有上の単一MD文書を編集する5端末以内。Windows 10／11、SMB 3系を検証対象とし、実測済みの共有構成を公開する。
- SMBはMD・assets・共同編集の復元用補助データの保存に使用し、入力ごとの編集同期はWebRTC DataChannelで行う。SMBのファイル監視をCRDT同期の代用にしない。
- 共同編集は明示的に開始・参加する。全参加者にSMB上の文書および補助領域の読み書き権限を要求する。読み取り専用参加は後続版。単独編集は通信サービスなしで従来どおり利用できる。
- 接続確立用に組織内シグナリングサービスを用意する。インターネットも公開シグナリングも必須にしない。STUN／TURNは初期状態で無効とし、組織指定の接続先のみ設定可能とする。TURN利用時は暗号化された通信が中継されることを示す。

## 10. AI共同編集（MCPサーバ）

### 10.1 目的と基本方針

- markink 本体が MCP（Model Context Protocol）サーバとなり、AI客户端が「人間の参加者と同じ立ち位置」で編集中のドキュメントに読み書きできる。
- AIは専用の中間ファイルや別プロセスを介さず、編集を人間の入力と同じ ProseMirror トランザクション経路で適用する。共同編集セッション有効時は Yjs に反映され WebRTC/中継で他端末へ同期される。単独編集時はローカル編集として扱う (MCP が Y.Doc を直接触る経路を作らない)。
- 同時に参加できるAIは **1セッションのみ**（1つでよい）。2つ目の接続要求は拒否し、UIに理由を表示する。
- AIのカーソル（remote cursor）には **AI名を表示する**（例: `AI: <モデル名 or クライアント名>`）。人間参加者のカーソルと視覚的に区別できる色を用いる。

### 10.2 接続と有効化

- MCPサーバは既定で **無効**。利用者が設定で「AI共同編集を有効にする」をONにしたときのみローカルで待ち受けを開始する。
- 待ち受けは **localhost（127.0.0.1）のみ**に限定し、LANやインターネットへは開放しない（他端末の人間参加は既存の9章WebRTCが担当し、AIは各マシン内で接続する）。
- 接続方式は2種類をサポートする。
  - stdio トランスポート（推奨・既定）: MCPクライアントが markink のサーバ実行ファイルをサブプロセスとして起動し、標準入出力でJSON-RPCをやり取りする。認証設定が不要で初心者に優しい。
  - Streamable HTTP トランスポート: ローカルHTTPエンドポイントに接続する。利用者は設定画面に表示される接続URL（と任意のトークン）をクライアントへコピーする。
- 有効化中、アプリは接続状態（待機中／接続済み／AI名）をUIに常時表示する。AIが切断したら待機中へ戻る。

### 10.3 MCPサーバが公開するツール

AIが人間と同じ操作を行えるよう、少なくとも次のツールを提供する（MCP Tool定義としてスキーマ付きで公開）。

- 読み取り
  - `get_document`: 現在のMarkdown全文（または選択範囲）を取得。
  - `search`: キーワード／正規表現で本文を検索し、位置を返す。
- 編集（人間入力と同じトランザクション経路で適用され、人間側Undo/Redoで取消可能）
  - `insert_text`: 指定位置（または末尾／見出し配下）にテキスト／Markdownを挿入。
  - `replace_range`: 指定範囲を置換。
  - `replace_all`: 全文置換。
  - `apply_markdown`: Markdown断片をパースして指定位置に適用（見出し・リスト・表・コード等を再現）。
- 構造操作
  - `get_outline`: 見出しツリー（レベル・見出し・位置）を返す。
  - `set_heading`: 対象ブロックの見出しレベルを設定。
- カーソル／選択（AIの意図を人間に可視化）
  - `set_cursor`: AIカーソル位置と選択範囲を指定。**AI名ラベル**がその位置に追従する。
  - `get_cursor`: 現在のAIまたはユーザーのカーソル位置を取得。
- 補助
  - `save_document`: 明示保存を要求（自動保存は既存機構で随時行う）。
  - `notify_human`: AIから人間への短いメッセージをトースト表示（任意）。
- 変更検知（AIが人間の編集を知るため）
  - `get_version`: 文書の版番号のみを返す軽量呼び出し（変更検知ポーリング用）。
  - `get_changes`: 指定版以降の変更を取得。変更がなければ`{changed:false}`のみ、あれば最新のMarkdown全文を返す。

### 10.4 編集モデルと整合性

- AIの編集は人間入力のときと同じ形で EditorView にトランザクションを dispatch して適用する (セッション中は y-prosemirror が Y.Doc へ反映し同期する。衝突は既存のCRDT解決に委ねる)。MCP ツール実行側が Y.Doc を直接触たり、ファイル保存を Rust 側へ直接要求する経路を持たない。
- AIの編集も履歴（Undo）対象に含める。人間が `Ctrl+Z` で直前のAI編集を取り消せること。
- AIの編集で自動保存・ファイル保存の既存フローを発火させる。
- 1文字単位で頻繁に送ると人間側が重くなるため、編集はブロック単位にバッチ適用する（パフォーマンス要件45〜47への配慮）。
- 文書変更の検知：人間の編集・他端末の編集・文書切替のたびに文書の版番号（version）を1つ進める。AI自身の編集も版を進めるが、通知の送信元（origin）で区別できる（`human` / `ai`）。通知は本文を含まず、変更のたびに送る（バックグラウンドでタイマーが止まる環境でも確実に届けるため、フロント側での間引きは行わない）。内容が必要なAIは`get_changes`で取りに行く。
  - stdio接続のAIにはサーバから`notifications/document/changed`通知を送る（人間のカーソル位置付き）。
  - HTTP接続には通知を送れないため、AIは`get_version`のポーリングで検知する。

### 10.5 セキュリティと権限

- MCPエンドポイントへ接続できるのはローカルプロセスのみ。リモートからのアクセスは受け付けない。
- HTTPトランスポート利用時はオプショナルな共有トークンを発行し、不一致の接続を拒否できる。
- AIの編集も人間と同一の権限モデルに置く。別ファイルへのアクセス、ファイルシステムの直接操作、ネットワーク送信はMCP経由では行わない（UIで明示）。
- 「AIによる自動保存」のON/OFFを利用者が設定できる（既定ON、`save_document`ツールは明示要求時のみ実行）。

### 10.6 UI要件

- 設定画面に「AI共同編集」セクションを新設: 有効化トグル、接続状態、接続方法（stdio/HTTP）、HTTP接続URLのコピー、任意トークン、参加中AI名、接続解除ボタン。
- AI接続中、ステータスバーに「AI: <名前>」のバッジを表示。
- AI名はツールバーの参加者一覧にも人間参加者と同列に表示し、AIであることを示すアイコンを付与。
- 2つ目のAIが接続を試みたときは、既存接続を優先し新しい接続を拒否、トーストで通知。

### 10.7 初期版の非対象

- 複数AIの同時参加、AI間の協調。
- AI向けの閲覧専用（読み取り専用）モード以外の細かいロール制御。
- 認証プロバイダ連携、リモート（LAN越え）からのMCP接続。
- 音声・画像入力を伴うAI機能（本文テキストの読み書きのみ）。


## 11. エディタ起点のAI呼び出し (opencode serve)

### 11.1 目的と基本方針

- MCPサーバ (AI→エディタ) とは逆方向に、エディタがAIに文章処理を依頼する。
- 用途: 要約・続き・質問・編集代行。会話の継続はしない (都度破棄)。
- localhost 完結。保存先以外にlocalhost外への送信はしない。

### 11.2 接続と有効化

- 既定無効。設定で有効にしたときのみ `opencode serve` を子プロセス起動し、localhost で待ち受ける。
- Basic認証パスワードは起動ごとに生成し、メモリ上でのみ保持する (ファイルに残さない)。
- 要求は session作成 → message送信 (応答待ち、`tools: {}` で道具呼び封印) → session削除 の3手。
- 同時実行は1件 (single-flight)。中断は abort。タイムアウト既定120秒。

### 11.3 UI仕様

- エディタ上の右クリックでAIメニューを出す: AI要約 / AI続き / AI質問 / AI編集代行 + メニュー内プロンプト入力欄 (質問・編集代行は必須)。
- 送信文字数をメニューに表示し、送信は明示操作のみ (自動送信なし)。
- 結果はダイアログ表示: [挿入する][選択範囲を置換][コピー][破棄する]。実行中は経過表示+中断。
- AI挿入は単一トランザクション (Undo一発)、`origin: ai` で版通知と整合。
